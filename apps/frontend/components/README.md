# Components

This folder will contain reusable presentational and composite UI components.

Future contents should focus on:
- shared interface building blocks
- layout primitives
- feature-level UI sections
- components that remain separate from page routing concerns
