# Hooks

This folder will contain reusable React hooks for the frontend application.

Examples of future responsibilities include:
- stateful UI helpers
- data-fetching wrappers
- browser capability integrations
- cross-page interaction logic that should not live inside components
