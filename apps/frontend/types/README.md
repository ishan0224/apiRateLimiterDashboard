# Types

This folder will contain frontend-specific TypeScript types and interfaces.

It is intended for:
- view model types
- component prop contracts
- frontend-only utility types
- shared type re-exports adapted for the UI layer
