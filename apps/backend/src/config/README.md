# Configuration

This folder will contain backend configuration modules.

It is intended for:
- environment variable parsing
- application configuration objects
- runtime feature flags
- central configuration helpers shared across the backend
