# Routes

This folder will contain Express route definitions for the backend API.

Routes should:
- define URL structure and HTTP methods
- compose middleware for validation, auth, and observability
- map incoming requests to controller functions
- keep transport wiring separate from business logic
